import { Key } from 'interface-datastore';
import { BaseDatastore } from './base.ts';
import type { Shard } from './index.ts';
import type { AbortOptions } from 'abort-error';
import type { Batch, KeyQuery, Pair, Query, Datastore } from 'interface-datastore';
/**
 * Backend independent abstraction of go-ds-flatfs.
 *
 * Wraps another datastore such that all values are stored
 * sharded according to the given sharding function.
 */
export declare class ShardingDatastore extends BaseDatastore {
    private readonly child;
    private shard;
    constructor(store: Datastore, shard: Shard);
    open(): Promise<void>;
    _convertKey(key: Key): Key;
    _invertKey(key: Key): Key;
    static create(store: Datastore, shard?: Shard): Promise<Shard>;
    put(key: Key, val: Uint8Array, options?: AbortOptions): Promise<Key>;
    get(key: Key, options?: AbortOptions): Promise<Uint8Array>;
    has(key: Key, options?: AbortOptions): Promise<boolean>;
    delete(key: Key, options?: AbortOptions): Promise<void>;
    putMany(source: Iterable<Pair> | AsyncIterable<Pair>, options?: AbortOptions): Generator<Key> | AsyncGenerator<Key>;
    getMany(source: Iterable<Key> | AsyncIterable<Key>, options?: AbortOptions): Generator<Pair> | AsyncGenerator<Pair>;
    deleteMany(source: Iterable<Key> | AsyncIterable<Key>, options?: AbortOptions): Generator<Key> | AsyncGenerator<Key>;
    batch(): Batch;
    query(q: Query, options?: AbortOptions): Generator<Pair> | AsyncGenerator<Pair>;
    queryKeys(q: KeyQuery, options?: AbortOptions): Generator<Key> | AsyncGenerator<Key>;
}
//# sourceMappingURL=sharding.d.ts.map